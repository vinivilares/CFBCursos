/* 
    Rolar a Pagina
    document.getElementById("btnPos").addEventListener("click", function rolar() {
    // window.scrollBy(x, y);
    window.scrollBy(500, 500);
}) */

let qtde = window.length;
let painel = document.getElementById("painel").innerHTML = qtde;

// for (let i = 0; i < qtde; i++) {
//     window.frames[i].location = "https://cfbcursos.com.br"
// }